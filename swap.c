#include "sort.h"

/**
 * swap - Function to swap two elements.
 * @a: First element.
 * @b: second element.
 * Return: Nothing.
 */

void swap_int(int a, int b)
{
	int temp;

	temp = a;
	a = b;
	b = temp;
	temp = 0;
}
